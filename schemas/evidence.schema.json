{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.v4-bu-autopilot.internal/schemas/evidence.schema.json",
  "title": "V4 BU Autopilot Evidence",
  "description": "Contrato universal para evidencias geradas pelas skills.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "evidence_id",
    "client_id",
    "source_type",
    "observed_at",
    "type",
    "statement",
    "confidence",
    "reference"
  ],
  "properties": {
    "evidence_id": {
      "type": "string",
      "minLength": 1
    },
    "client_id": {
      "type": "string",
      "minLength": 1
    },
    "source_type": {
      "type": "string",
      "minLength": 1
    },
    "source_name": {
      "type": ["string", "null"]
    },
    "source_date": {
      "description": "Data explicitamente sustentada pela PROPRIA FONTE (ex.: data de emissao de um documento, data de um fechamento mensal citado no texto). Conceito distinto de 'observed_at'. Se a fonte nao informa sua propria data, este campo deve ser null - nunca preenchido com a data de execucao da skill, nem inferido do conteudo. Ver CLAUDE.md secao 26.3.",
      "type": ["string", "null"],
      "format": "date-time"
    },
    "observed_at": {
      "description": "Timestamp de execucao: momento real em que o sistema leu/observou esta evidencia, obtido do relogio do sistema no momento da execucao (UTC RFC3339) - nunca estimado, arredondado, inferido do conteudo da fonte, ou definido como horario futuro. Conceito distinto de 'source_date' (data que a propria fonte declara). Ver CLAUDE.md secao 26.",
      "type": "string",
      "format": "date-time"
    },
    "type": {
      "type": "string",
      "enum": [
        "fact",
        "metric",
        "decision",
        "hypothesis",
        "request",
        "commitment",
        "pending",
        "risk",
        "idea",
        "dependency"
      ]
    },
    "statement": {
      "type": "string",
      "minLength": 1
    },
    "confidence": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ]
    },
    "reference": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "label"
      ],
      "properties": {
        "label": {
          "type": "string",
          "minLength": 1
        },
        "uri": {
          "type": ["string", "null"]
        },
        "path": {
          "type": ["string", "null"]
        },
        "location": {
          "type": ["string", "null"]
        }
      }
    },
    "value": {
      "type": [
        "string",
        "number",
        "integer",
        "boolean",
        "null"
      ]
    },
    "unit": {
      "type": ["string", "null"]
    },
    "period": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "properties": {
        "from": {
          "type": ["string", "null"],
          "format": "date"
        },
        "to": {
          "type": ["string", "null"],
          "format": "date"
        }
      }
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    }
  }
}
